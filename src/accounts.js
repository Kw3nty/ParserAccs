async function loadAccounts(onProgress){const bmData=await fetchJson(`https://graph.facebook.com/v25.0/me/businesses?fields=id,name&limit=500&access_token=${encodeURIComponent(token)}`);const businesses=bmData.data||[];const accountToBM={};let bmDone=0;for(const bm of businesses){try{const[cl,ow]=await Promise.all([fetchJson(`https://graph.facebook.com/v25.0/${bm.id}/client_ad_accounts?fields=id&limit=500&access_token=${encodeURIComponent(token)}`).catch(()=>({data:[]})),fetchJson(`https://graph.facebook.com/v25.0/${bm.id}/owned_ad_accounts?fields=id&limit=500&access_token=${encodeURIComponent(token)}`).catch(()=>({data:[]}))]);[...cl.data,...ow.data].forEach(acc=>{if(!accountToBM[acc.id])accountToBM[acc.id]={id:bm.id,name:bm.name};});}catch(e){}bmDone++;if(onProgress)onProgress('BM: '+bmDone+'/'+businesses.length);}const data=await fetchJson(`https://graph.facebook.com/v25.0/me/adaccounts?fields=id,name,account_status,balance,currency,business,timezone_name,adtrust_dsl,account_currency_ratio_to_usd,adspaymentcycle{threshold_amount}&limit=500&access_token=${encodeURIComponent(token)}`);if(data.error)throw new Error(data.error.message);const accs=data.data||[];accs.forEach(a=>{if(accountToBM[a.id])a.accessibleBM=accountToBM[a.id];a.ratio=a.account_currency_ratio_to_usd||1;});let done=0;const total=accs.length*3;await runBatched(accs,12,async(a)=>{const tz=a.timezone_name||undefined;const initRanges=[getRange('today',tz),getRange('yesterday',tz),getRange('last_7_days',tz)];const[ads,sp,life]=await Promise.all([getAds(a.id),getInsightsMulti(a.id,initRanges),getInsightsPreset(a.id,'maximum')]);done+=3;if(onProgress)onProgress(done+'/'+total);a.ads=ads;a.dailyLimit=a.adtrust_dsl?Math.round(a.adtrust_dsl/a.ratio):0;a.billing=a.adspaymentcycle&&a.adspaymentcycle.data&&a.adspaymentcycle.data[0]?Math.round(a.adspaymentcycle.data[0].threshold_amount/100/a.ratio):0;a.spend={today:sp[0],yesterday:sp[1],last7Days:sp[2],lifetime:life};a.periodSpend=a.spend.last7Days;a._periodKey='last_7_days';});return accs;}let loadOv=null,loadOvSt=null;function showLoadOv(title){hideLoadOv();loadOv=document.createElement('div');loadOv.className='pa-overlay';loadOv.style.zIndex='1000003';loadOv.innerHTML='<div class="pa-load-box"><div class="pa-load-title">'+(title||t('loading'))+'</div><div class="pa-load-spinner"></div><div id="pa-load-st" style="margin-top:15px;font-size:12px;color:#9aa0ab;font-family:ui-monospace,monospace;">0/0</div></div>';document.body.appendChild(loadOv);loadOvSt=document.getElementById('pa-load-st');}function setLoadOv(txt){if(loadOvSt)loadOvSt.textContent=txt;}function hideLoadOv(){if(loadOv){loadOv.remove();loadOv=null;loadOvSt=null;}}showLoadOv(t('loading'));loadAccounts(txt=>setLoadOv(txt)).then(accs=>{hideLoadOv();if(!accs||!accs.length){alert(t('no_accs'));return;}showModal(accs);}).catch(e=>{hideLoadOv();alert(t('err')+(e&&e.message?e.message:e));});
